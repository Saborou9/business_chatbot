<nav class="bg-black border-b border-gray-800">
  <div class="mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 items-center justify-between">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <%= link_to "BusinessBuddy", root_path, class: "text-xl font-bold text-white" %>
        </div>
      </div>
      <div class="flex-1 flex justify-center">
        <div class="hidden md:flex items-center space-x-8">
        <% if user_signed_in? %>
          <%= link_to "Chat", chats_path, class: "text-white hover:text-green-400 px-3 py-2 rounded-md text-sm font-medium" %>
        <% else %>
          <%= link_to "Features", "#", class: "text-white hover:text-green-400 px-3 py-2 rounded-md text-sm font-medium" %>
          <%= link_to "About", about_path, class: "text-white hover:text-green-400 px-3 py-2 rounded-md text-sm font-medium" %>
          <%= link_to "Pricing", pricing_path, class: "text-white hover:text-green-400 px-3 py-2 rounded-md text-sm font-medium" %>
        <% end %>
        </div>
      </div>
      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <%= link_to "Account", edit_user_registration_path, class: "text-white hover:text-green-400 px-3 py-2 rounded-md text-sm font-medium" %>
          <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete }, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-green-600 hover:bg-green-700" %>
        <% else %>
          <%= link_to "Log in", new_user_session_path, class: "text-white hover:text-green-400 px-3 py-2 rounded-md text-sm font-medium" %>
          <%= link_to "Sign up", new_user_registration_path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-green-600 hover:bg-green-700" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
