<!DOCTYPE html>
<html class="h-full bg-black overflow-hidden">
  <head>
    <title><%= content_for(:title) || "BusinessBuddy" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full flex flex-col m-0 p-0 overflow-hidden">
    <% unless user_signed_in? && controller_name == 'chats' %>
      <%= render 'layouts/navigation' %>
    <% end %>
    
    <main class="flex-1 overflow-y-auto">
      <% if notice %>
        <p class="notice"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert"><%= alert %></p>
      <% end %>
      
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-6 min-h-full">
        <%= yield %>
      </div>
    </main>

    <%= render "chats/new_chat_modal" if user_signed_in? %>
  </body>
</html>
