<section class="bg-black min-h-screen flex items-center">
  <div class="container mx-auto px-4">
    <div class="max-w-md mx-auto bg-gray-900 p-8 rounded-lg shadow-lg">
      <h2 class="text-3xl font-bold text-green-500 mb-8 text-center">Log In</h2>
      <%= render "devise/sessions/new" %>
      <div class="mt-6 text-center">
        <p class="text-sm text-gray-400">
          Don't have an account? 
          <%= link_to "Sign up", new_user_registration_path, class: "font-medium text-green-500 hover:text-green-400" %>
        </p>
      </div>
    </div>
  </div>
</section>
