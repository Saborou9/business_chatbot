<% content_for :title, "Chats" %>

<% content_for :title, "Chats" %>

<div class="w-full">
  <h1 class="font-bold text-4xl mb-6">Chat Details</h1>
  
  <% if @chats.any? %>
    <div id="chat-content">
      <!-- Your main chat content goes here -->
      <!-- This could be the content of the selected chat -->
    </div>
  <% else %>
    <p class="text-center my-10">No chat selected.</p>
  <% end %>
</div>
